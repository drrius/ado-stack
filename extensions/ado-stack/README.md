# ado-stack

A sidebar tree over the `ado-stack` CLI. Every action shells out to `ado-stack`. There is no second copy of stack, Git, or Azure DevOps logic.

Requires `ado-stack` on the PATH. If the repository has no state, the extension offers **Run init**.
